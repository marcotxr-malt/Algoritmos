Algoritmo "circulo"

Var

   area, constante, raio : real

Inicio

      escreval("Digite o valor do raio do circulo: ")
      leia(raio)
      
      constante <- Pi
      area <- constante * (Exp(raio, 2))
      
      escreval("AREA = ", area:8:3)

Fimalgoritmo